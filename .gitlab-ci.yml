image: gradle:alpine

variables:
  path: "/builds/$CI_PROJECT_PATH"

before_script:
  - export GRADLE_USER_HOME=$path/.gradle

stages:
  - build

build:
  stage: build
  script:
    - gradle assemble
    - ls -lR
    - mv $path/build/libs/*.jar $path/
  artifacts:
    paths:
      - $path/*.jar
